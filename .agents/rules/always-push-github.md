# Always Push to GitHub

- After completing and verifying any code modifications, bug fixes, or feature updates in the workspace, automatically stage, commit, and push the changes to GitHub (`git add`, `git commit`, `git push origin <branch>`).
- Use descriptive conventional commit messages (e.g., `fix(ui): ...`, `feat(hero): ...`).
